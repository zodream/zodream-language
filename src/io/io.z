a // public a

_ b // private b

: c  // static c

()   // 初始化
    v = 1
    if true
        v ++

(_ a, _ b = 1)  // 初始化并设置全局变量
    a + b

_ a(b)           // 私有方法
    d, _ = d(3)
    ret b(b)

: b(b)           // 静态方法
    ret ++ b

d(c)
    ret c + 1, c + 2