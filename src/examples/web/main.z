
import (
    fmt
    net/http/Server
)

pub fn Main(args string[]) {
    if args.length > 0 {
        fmt.Printf("123123")
    }
    app := Server.New()
    app.Party('app', () => {
        app.Handle("/", )
    })
    app.Listen(port=8080)
}
