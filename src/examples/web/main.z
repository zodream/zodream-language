
import (
    fmt
    net/http/Server
)

pub fn Main(args string[]) {
    if args.length > 0 {
        fmt.Printf("123123")
    }
    app := Server.New()
    Router.Register(app)
    app.Listen(port=8080)
}
